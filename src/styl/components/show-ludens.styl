.show-ludens {
  text-align: center;
  .title {
    font: 700 36px $font-base;
    text-transform uppercase;
    letter-spacing: 0.2em;
    color: $primary;
    max-width: 350px;
    margin: 0 auto;
  }
  .subtitle {
    font: 700 24px $font-base;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    color: $white;
    margin: 50px auto;
  }
}